add_library(SmartDeviceLib
    smartDevice.cpp
    smartDevice.h
)

target_include_directories(DeviceLib PUBLIC
    ${CMAKE_CURRENT_SOURCE_DIR}
)
target_link_libraries(SmartDeviceLib PUBLIC
    DeviceLib
)

target_compile_features(DeviceLib PUBLIC
    cxx_std_17)